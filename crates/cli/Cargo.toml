[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
core-db = { path = "../core-db" }
ml-models = { path = "../ml-models" }
anyhow = "1"
clap = { version = "4.6.0", features = ["derive"] }
